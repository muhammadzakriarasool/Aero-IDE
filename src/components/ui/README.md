/**
 * @file src/components/ui/.gitkeep
 * @description Reserved for Shadcn UI components (Phase 2).
 *
 * To add Shadcn UI, run:
 *   npx shadcn@latest init
 *
 * Components will be generated into this directory.
 * Shadcn requires `clsx` and `tailwind-merge` — add them at that point.
 */
